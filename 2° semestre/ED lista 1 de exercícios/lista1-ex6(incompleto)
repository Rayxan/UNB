#include<stdio.h>
#include<string.h>
#define MAX 100
int main(){
	char frase[MAX];
	int i=0;
	
	scanf("%[^\n]", frase);
	
	while(frase[i] != '\0'){
		
		printf("%c;",frase[i]);
		/*if(frase[i] == '\0' ){
			frase[i-1] = ' ';
		}*/
		i++;
	}
	return 0;
}
/*usar getchar talvez*/
